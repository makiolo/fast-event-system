language: c
os: linux
compiler:
  - clang
  - gcc
install:
  - pip install --user conan
env:
  - MODE=Debug
  - MODE=Release

# COMPILER=gcc COMPILER_VERSION=4.8 COMPILER_LIBCXX=libstdc++

script:
  - npm install
  - npm test
