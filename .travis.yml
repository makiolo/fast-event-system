language: c
os: linux
compiler:
  - clang

install:
  - pip install --user conan

env:
  - MODE=Debug COMPILER=clang COMPILER_VERSION=5.0 COMPILER_LIBCXX=libstdc++11
  - MODE=Release COMPILER=clang COMPILER_VERSION=5.0 COMPILER_LIBCXX=libstdc++11

script:
  - npm install
  - npm test
